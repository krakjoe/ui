// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ui", "for ui support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ui", "enable ui support", "no");

if (PHP_UI != "no") {
	EXTENSION("ui", "ui.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

